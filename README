Installation:

    git clone git://github.com/EiyuuZack/dotvim.git ~/.vim

Create symlink:

    ln -s ~/.vim/vimrc ~/.vimrc

Switch to the `~/.vim` directory, and fetch submodules:

    cd ~/.vim
    git submodule update --init

Update submodules:

    git submodule foreach git pull
